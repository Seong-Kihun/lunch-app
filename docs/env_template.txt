# 환경 변수 설정 예시
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# 데이터베이스 설정
DATABASE_URL=sqlite:///site.db
# 프로덕션에서는 PostgreSQL 사용:
# DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/lunch_app

# 보안 키 (반드시 변경하세요!)
SECRET_KEY=your-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here

# 이메일 설정 (선택사항)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
INQUIRY_EMAIL=inquiry@yourcompany.com

# CORS 설정 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:19006,http://127.0.0.1:19006

# Flask 환경
FLASK_ENV=development
FLASK_DEBUG=True

# 포트 설정
PORT=5000

# Python 경로 (Render에서 사용)
PYTHONPATH=/opt/render/project/src